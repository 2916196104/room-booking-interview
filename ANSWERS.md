# 第一题：时间窗口规则

这题主要是检查预约时间合不合法。需要注意的是检查顺序，还有 30 分钟和 120 分钟这两个边界。

## 思路简要

题目已经给出了判断顺序，所以直接按顺序写几个 `if` 就可以，满足条件就返回对应结果：

1. 开始或结束时间没填：返回 `MISSING_BOUNDARY`。
2. 结束时间等于或早于开始时间：返回 `END_NOT_AFTER_START`。
3. 时长不到 30 分钟，或者超过 120 分钟：返回 `DURATION_OUT_OF_RANGE`。
4. 都没问题：返回 `VALID`。刚好 30 分钟和 120 分钟也算合法。

先判断空值，是因为后面要比较两个时间，如果其中一个是 `null`，直接比较就会出现空指针异常。

接着用 `!end.isAfter(start)` 判断结束时间是否不晚于开始时间。这里包含“早于”和“等于”，如果只判断“早于”，就会漏掉两个时间相同的情况。

时长用 Java 自带的 `Duration.between(start, end)` 计算，再用 `compareTo` 和 30、120 分钟比较。比较结果小于零表示更短，等于零表示一样长，大于零表示更长。代码只排除小于 30 分钟和大于 120 分钟的情况，所以刚好等于边界也能通过。

不能先用 `toMinutes()` 转成整数分钟。比如 120 分 1 秒会变成 120 分钟，这样就可能把超时的预约放过去。直接比较 `Duration` 能保留秒和纳秒，也不用另外加依赖。

这个结果会被 `BookingService.isAvailable` 使用。时间不合法就通过现有异常处理返回 HTTP 400 和 `INVALID_BOOKING_WINDOW`；时间合法才继续检查预约冲突。所以返回 `VALID` 只说明时间没问题，还不能说明房间一定有空。

## 测试思路

测试主要围绕题目的几条规则来选数据。除了空值、时间相同和时间倒序，还要检查刚好 30、120 分钟，以及比它们少一点、多一点的情况。日期都用题目里的 `2030-01-15`，下表省略日期。

| start | end | 预期结果 | 原因 |
|---|---|---|---|
| null | 09:30:00 | `MISSING_BOUNDARY` | 开始时间没填 |
| 09:00:00 | null | `MISSING_BOUNDARY` | 结束时间没填 |
| null | null | `MISSING_BOUNDARY` | 两个都没填 |
| 09:00:00 | 09:00:00 | `END_NOT_AFTER_START` | 时间相同，先报时间顺序问题 |
| 09:00:00 | 08:59:00 | `END_NOT_AFTER_START` | 结束更早，先报时间顺序问题 |
| 09:00:00 | 09:29:59 | `DURATION_OUT_OF_RANGE` | 不到 30 分钟 |
| 09:00:00 | 09:30:00 | `VALID` | 刚好 30 分钟 |
| 09:00:00 | 09:30:01 | `VALID` | 30 分 1 秒，在范围内 |
| 09:00:00 | 10:59:59 | `VALID` | 119 分 59 秒，在范围内 |
| 09:00:00 | 11:00:00 | `VALID` | 刚好 120 分钟 |
| 09:00:00 | 11:00:01 | `DURATION_OUT_OF_RANGE` | 超过 120 分钟 |
| 09:00:00 | 10:00:00 | `VALID` | 普通的 60 分钟预约 |
| 09:00:00 | 09:29:59.999999999 | `DURATION_OUT_OF_RANGE` | 比下限少 1 纳秒 |
| 09:00:00 | 11:00:00.000000001 | `DURATION_OUT_OF_RANGE` | 比上限多 1 纳秒 |

运行前选了“120 分 1 秒”这个例子判断预期结果：两个时间都有，结束比开始晚，但多了 1 秒，已经超过上限，所以应该返回 `DURATION_OUT_OF_RANGE`。

`09:00 → 08:59` 用来检查规则的先后顺序。它既是结束比开始早，时长也小于 30 分钟。按题目要求，要先返回 `END_NOT_AFTER_START`。如果把时长检查放在前面，这个测试就会失败。

测试沿用项目里的 JUnit 5，用参数化测试把这 14 组数据逐一跑一遍，省得重复写 14 个测试方法。`@CsvSource` 里每行是一组数据，不加引号并留空的一项表示 `null`。最后用 `assertEquals` 检查返回值是不是预期结果。这里只测时间判断方法，不用启动整个 Spring 应用。

## 改错一个条件会怎样

比如把上限判断的 `> 0` 改成 `>= 0`，刚好 120 分钟也会被拒绝，就不符合题意了。

实际改动后，第 10 组 `exactly 120 minutes` 测试失败，预期是 `VALID`，实际返回 `DURATION_OUT_OF_RANGE`，其余 13 组通过。恢复 `> 0` 后，第一题的 14 个测试和全部 16 个测试都通过了。

## AI 使用说明

本题使用了 AI 辅助分析和整理说明，代码与测试的编写、测试执行及环境排查也由 AI 协助完成。
