# 第一题：时间窗口规则

## 实现思路

按题目要求依次检查，发现问题就返回：

1. 开始或结束时间没填：MISSING_BOUNDARY。
2. 结束时间等于或早于开始时间：END_NOT_AFTER_START。
3.

|时长不足|30 分钟或超过|120 分钟：DURATION_OUT_OF_RANGE。||
|---|---|---|---|
|其余情况：VALID，刚好||30 分钟和|120 分钟也合法。|

4.
先检查有没有填时间，避免后面比较时出错。判断结束时间时使 用 !end.isAfter(start)，同时包含“早于”和“等于”。 时长用 Duration.between(start, end) 计算，再直接与上下限比较。不先转成 整数分钟，否则“120 分 1 秒”会被算成 120 分钟，错误地通过检查。 返回 VALID 只说明时间合法，不代表房间有空。后面还要检查是否与已有预约 冲突。

## 测试与结果

使用 JUnit 5，日期统一为 2030-01-15，共测试 14 组数据：

- 开始时间为空、结束时间为空、两者都为空。
- 开始和结束相同、结束早于开始。

|• 29 分|59 秒、30|分钟、30|分 1 秒。||
|---|---|---|---|---|
|• 119 • 普通的|分 59 秒、120 60 分钟。|分钟、120|分 1|秒。|
|• 比 30|分钟少|1 纳秒、比|120 分钟多|1 纳秒。|

运行前，选“120 分 1 秒”推导结果：时间都已填写，先后顺序正确，但超过上 限，应返回 DURATION_OUT_OF_RANGE。 “09:00 → 08:59”同时存在时间倒序和时长不足的问题，应先返回 END_NOT_AFTER_START，用来检查判断顺序。 还实际把上限条件从 > 0 改成了 >= 0。这样刚好 120 分钟也会被拒绝，对应 测试失败，其余 13 组通过。恢复后，本题 14 组测试及项目全部 16 个测试通 过。


# 第二题：找不到预约时返回 404

## 排查思路

请求 GET /rooms/room-101/bookings/booking-missing 时，房间存在，但预约 不存在。原来返回 500，应该改为 404。 修改前考虑了三个原因：

1. 参数传错或查询条件有误。 检查 Controller、Service、Repository 之间传递的两个 ID 及顺序。
2. 查不到预约后出现意外异常。 检查空结果的处理方式，并查看测试中实际出现的异 常。
3. 异常处理时选错状态码。 检查处理方法，再对照测试中的状态码和响应内容。
   检查发现，虽然不同方法的参数顺序不同，但调用时已经对应调整，没有传反。 查不到预约时，Service 会抛出 BookingNotFoundException，测试中实际出现的 也是这个异常。 问题在 handleBookingNotFound：它处理的是预约不存在，却使用了 HttpStatus.INTERNAL_SERVER_ERROR，因此返回了 500。 以上调用关系来自源码分析，异常和响应来自 MockMvc 测试。本次没有完成 **IDEA GUI** 单步调试，不能把这些结果当成调试器中的观察记录。

## 修改与验证

业务代码只改了一行： HttpStatus.INTERNAL_SERVER_ERROR 改为： HttpStatus.NOT_FOUND 继续使用已有错误格式，保留 status、error、code、message、path 五个字段， 错误码仍为 BOOKING_NOT_FOUND。查询逻辑不需要修改。

先在 BookingApiTest 中新增测试，检查 HTTP 状态、错误内容和响应格式， 并确认没有异常类名或调用栈。 修改前运行测试，因实际返回 500 而失败。改成 404 后运行 mvn test，接口 测试 3 个、第一题测试 14 个，共 17 个全部通过。 这里要同时检查真正的 HTTP 状态和 JSON 中的 status，不能只改其中一个。 现有公共方法使用同一个状态参数设置两处，因此改这一行即可。 如果把状态码改回去，HTTP 状态和 JSON 中的 status 都会重新变成 500，新 增测试也会再次失败。 以上是修复时记录的测试结果，本次整理文字没有重新运行测试。手动请求前， 需要重启应用，让新代码生效。

## 为什么用 404，不用 400

这次请求可以正常解析，只是找不到指定预约，所以返回 404。400 表示请求本 身有问题，例如缺少必要参数、JSON 写错或时间格式不对。 其他常见状态码：

- **401**：没有提供有效的登录凭据。
- **403**：不允许访问，例如没有管理员权限。
- **405**：接口不支持当前请求方法。
- **409**：与已有数据冲突，例如预约时间重叠。
- **415**：不支持提交的数据类型，例如只接受 JSON，却提交 XML。
- **429**：请求过于频繁，触发限制。

# 第三题：修复可用性判断

## 问题原因

查询 room-202 的 10:15～10:45 时，与已有的 10:00～10:30 预约重叠，应该返 回不可用，原来却返回可用。 修改前考虑了几个原因：时间解析错误、查错房间、重叠条件写错，或者返回值 取反有误。通过检查参数、预约数据和返回过程，发现问题出在循环中： hasConflict = overlaps; 这行代码每次都会覆盖之前的结果。第一条预约有冲突，结果变成 true；第二条 12:00～12:30 没有冲突，又把结果改成 false，最后就错误地返回了可用。重叠条 件本身没有问题。

## 修改过程

先新增 15 组测试，覆盖部分重叠、完全相同、包含、首尾相接、空档和不同房 间。修改前 4 组失败，都是前面的预约有冲突，却被后面的结果覆盖。 最初改为： hasConflict = hasConflict || overlaps; 这样只要发现过冲突，后面就不会把它清掉。修改后 15 组测试通过，当时全部 32 个测试也通过。 后来改用 anyMatch，直接在仓库中查找同一房间是否存在冲突，找到一条就结 束，不再先生成预约列表。服务层把“有冲突”取反，得到“是否可用”。 重叠需要同时满足：请求开始早于已有预约结束，已有预约开始早于请求结束。 比较不包含相等，所以 10:00～10:30 和 10:30～11:00 可以接着预约。原来的房 间和时间检查继续保留。

## 测试结果

又补了仓库测试，检查没有预约、新增预约后发生冲突、首尾相接、不同房间互 不影响，以及查询不会改动数据。

最终用已安装的 Maven 和 JDK 21 运行测试，相关测试 19 个、全部测试 33 个均 通过。接口测试不仅检查请求成功，还检查返回的可用性是否正确。 如果恢复原来的覆盖写法，前 4 组测试会再次失败。按代码推导，去掉返回值前 的 ! 会把结果颠倒；去掉房间判断会让不同房间互相影响。这两种改动没有实 际执行。

## 数据很多时怎么办

当前优化只是少建列表、发现冲突提前结束。没有冲突时，仍可能扫描仓库全部 记录，也仍然占用内存。 如果数据存在数据库中，应让数据库直接判断是否存在冲突，只返回结果，不要 把数十亿条预约加载到 Java 里。再根据房间和时间条件设计索引，检查实际查 询速度。加索引不保证一定快，需要用真实规模的数据验证。

# 第四题：创建预约

思路是先检查，再保存。补上请求对象、POST入口和创建逻辑，复用原有校验。

请求record接收起止时间，不接收ID。@PostMapping接收POST；@PathVariable取房间ID；@RequestBody将JSON转成对象。create传入房间和时间，from把结果转成响应。

!isAvailable表示不可用：先查房间，再查时间是否齐全、结束更晚、时长为30到120分钟，最后查冲突。冲突时throw停止执行，通过才调用仓库create。异常处理沿用400时间非法、404房间不存在、409预约冲突及原错误码。

重叠要求同一房间、新开始早于旧结束、旧开始早于新结束。比较不含等于，所以首尾相接不冲突。仓库getAndIncrement取编号再加一，new Booking创建对象，put存入内存，return返回。检查都在保存前，失败不会写数据。

fromCurrentRequest取请求地址，path追加ID占位符，buildAndExpand填入ID，toUri转成URI。created设置201和Location，body放入预约内容，供客户端查询。

测试每次新建仓库。成功时检查响应字段、ID、201和Location，再查询保存结果；失败时检查错误码，并比较数量和原记录，防止多存或覆盖。覆盖时长边界、相差1纳秒、首尾相接、跨房间及重复提交。此前新增36个测试，全量69个通过；真实POST返回201，沿Location查询为200且内容相同。

如果删除创建方法中的if和throw，只保留保存，会跳过校验，非法和冲突预约也会写入，测试会失败。这是推导，未实际删除。

新增预约由服务端生成ID，所以选POST。PUT在指定地址创建或整体替换资源。幂等指重复请求的预期影响与一次相同，不要求响应一样。PUT幂等，POST不保证；本接口用409拒绝重复预约，是业务规则。GET读取，DELETE删除，HEAD只取响应头，OPTIONS查询支持的操作，都幂等。

PATCH通常只改部分内容，不保证幂等：设成固定值可以幂等，每次加一就不行。
